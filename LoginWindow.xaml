<Window x:Class="экзамка.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:экзамка"
        mc:Ignorable="d"
        Title="Авторизация" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Языки народов мира - Информационная система" 
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Логин:" Width="80" FontSize="14" VerticalAlignment="Center"/>
            <TextBox x:Name="txtLogin" Width="200" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Пароль:" Width="80" FontSize="14" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtPassword" Width="200" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center">
            <Button Content="Войти" Width="100" Height="30" Click="BtnLogin_Click" Margin="0,0,10,0"/>
            <Button Content="Гость" Width="100" Height="30" Click="BtnGuest_Click" Margin="0,0,10,0"/>
            <Button Content="Выход" Width="100" Height="30" Click="BtnExit_Click"/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="4" Text="" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10"/>
    </Grid>
</Window>
